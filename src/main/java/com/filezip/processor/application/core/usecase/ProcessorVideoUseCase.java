package com.filezip.processor.application.core.usecase;

import com.filezip.processor.application.core.domain.VideoFrame;

public interface ProcessorVideoUseCase {

    void process(VideoFrame videoFrame);
}
