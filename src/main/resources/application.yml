spring:
  application:
    name: batch-processor
  data:
    mongodb:
      uri: mongodb+srv://admin:AdminPassword123@deliverynow-cluster.gg846.mongodb.net/?retryWrites=true&w=majority&appName=deliverynow-cluster
      database: deliverynow-db
server:
  port: 5001

resources:
  temp:
    video: src/main/resources/temp
    images: src/main/resources/images
    zip: src/main/resources/arquivo

aws:
  sqs:
    queue-name: filezip-process-queue

